{
  "name": "@vitexte/api",
  "version": "1.0.0",
  "scripts": {
    "clean": "rm -rf ./dist",
    "compile": "tsc -p tsconfig.build.json",
    "build": "yarn run clean && yarn run compile"
  },
  "devDependencies": {
  },
  "dependencies": {
    "zod": "^3.11.6"
  }
}
